# Python / uv
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/

# Large trajectory data — the full episode (with camera images) is ~200 MB; keep it out of git.
data/*.npz
# ...except the small state-only "slim" episode (~2 MB), which IS committed.
!data/*_slim.npz
# Replay-generated mocap sidecars (incl. *_slim_mocap.npz) stay out.
*_mocap.npz

# MuJoCo
MUJOCO_LOG.TXT

# Editor / OS
.DS_Store
.idea/
.vscode/
